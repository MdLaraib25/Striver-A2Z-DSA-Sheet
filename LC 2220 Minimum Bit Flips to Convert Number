class Solution {
    public int minBitFlips(int start, int goal) {
        int result = start ^ goal;
        int cnt = 0;
        while(result != 0){
            cnt += (result & 1);
            result  >>= 1;
        } return cnt;
    }
}
