class Solution {
    public long maximumHappinessSum(int[] happiness, int k) {
        Arrays.sort(happiness);
        long total = 0;
        int turn = 0;

        for(int i = happiness.length - 1; i >= 0 && k > 0; i--){
            int joy = happiness[i] - turn;
            if(joy > 0) total += joy;
            turn++;
            k--;
        } return total;
    }
}
